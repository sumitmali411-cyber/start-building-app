# vitest 4 + @vitest/browser-playwright declare a peer set npm cannot resolve
# strictly (npm crashes with "edgesOut" on strict resolution). Pin the legacy
# resolver so install and ci behave the same for everyone.
legacy-peer-deps=true
